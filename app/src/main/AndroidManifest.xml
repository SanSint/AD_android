<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.san.logicuniversity_ad">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Store_StockChecking" />
        <activity android:name=".Store_StockCheckList" />
        <activity android:name=".Store_Success" />
        <activity android:name=".Store_ViewDisbursementList" />
        <activity android:name=".Store_DisbursementList" />
        <activity android:name=".Store_RetrivalForm" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".StoreClerk_menu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>