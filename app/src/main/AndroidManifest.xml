<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.san.logicuniversity_ad">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StockChecking" />
        <activity android:name=".StockCheckList" />
        <activity android:name=".Success" />
        <activity android:name=".ViewDisbursementList" />
        <activity android:name=".DisbursementList" />
        <activity android:name=".RetrivalForm" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".StoreClerk_menu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>